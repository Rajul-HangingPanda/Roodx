import { apiSlice } from '@/store/api/apiSlice';
// import { IUser, IAuth } from '@/interface/Auth.interface';
//
import {} from '@/store/slices/authSlice';

export const authApi = apiSlice.injectEndpoints({
  endpoints: (builder) => ({}),
});

export const {} = authApi;
